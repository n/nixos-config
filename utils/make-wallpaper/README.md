# make-wallpaper

Generates an ultrawide (3440x1440) wallpaper from the [NixOS artwork repository](https://github.com/NixOS/nixos-artwork/).

## Usage

```sh
nix develop
make-wallpaper [output-path]
```

Defaults to `users/nick/dotfiles/.background-image`.
